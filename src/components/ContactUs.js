import { React, Component } from 'react';

class ContactUs extends Component {
    constructor(props) {
        super(props);
    }
    state = {  }
    render() { 
        return ( 
            <button>This is where the Contact button is going to go</button>
         );
    }
}
 
export default ContactUs;